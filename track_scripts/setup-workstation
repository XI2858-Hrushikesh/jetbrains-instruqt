#!/bin/bash
# Track-level setup: everything is pre-installed in the Docker image.
# The desktop (Xvfb + XFCE + noVNC) starts automatically via the container CMD.
# Allow extra time for IntelliJ to load before the first challenge.

sleep 30

echo "Track environment ready."
exit 0
